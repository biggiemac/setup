Vagrant and Virtualbox are required for this example to work.

1. Clone this repo.
2. run 'vagrant up'
3. Verify locally inside the box via 'curl localhost:8000' or externally from your browser via 192.168.33.10:8000
